import { RedondearPrecioPipe } from './redondear-precio.pipe';

describe('RedondearPrecioPipe', () => {
  it('create an instance', () => {
    const pipe = new RedondearPrecioPipe();
    expect(pipe).toBeTruthy();
  });
});
