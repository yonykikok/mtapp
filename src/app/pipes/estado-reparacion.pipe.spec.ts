import { EstadoReparacionPipe } from './estado-reparacion.pipe';

describe('EstadoReparacionPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoReparacionPipe();
    expect(pipe).toBeTruthy();
  });
});
