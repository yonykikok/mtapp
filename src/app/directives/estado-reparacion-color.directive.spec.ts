import { EstadoReparacionColorDirective } from './estado-reparacion-color.directive';

describe('EstadoReparacionColorDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoReparacionColorDirective();
    expect(directive).toBeTruthy();
  });
});
