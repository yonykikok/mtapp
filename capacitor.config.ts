import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jcode.multitask',
  appName: 'MTApp-update',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
