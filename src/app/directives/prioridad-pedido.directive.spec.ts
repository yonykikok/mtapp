import { PrioridadPedidoDirective } from './prioridad-pedido.directive';

describe('PrioridadPedidoDirective', () => {
  it('should create an instance', () => {
    const directive = new PrioridadPedidoDirective();
    expect(directive).toBeTruthy();
  });
});
